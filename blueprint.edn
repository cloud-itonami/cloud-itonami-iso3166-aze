{:itonami.blueprint/id "cloud-itonami-iso3166-aze"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Republic of Azerbaijan"
 :itonami.blueprint/iso3166 "AZE"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/social-impact [:sme-market-access :public-spend-transparency :cross-border-friction-reduction]
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "Full Actors-pattern implementation: MarketEntry-LLM advisor (marketentry.marketentryllm) sealed behind an independent Market-Entry Compliance Governor (marketentry.governor, unreliable-supplier-listed flagship check -- independently recomputes etibarsız təchizatçılar reyestri (unreliable-supplier registry, Cabinet of Ministers Decision No. 492, 30.12.2023) membership rather than trusting a proposal's own self-report, plus spec-basis / evidence-incomplete / engagement-fee-mismatch / voen-unverified / already-drafted / already-submitted) driving a langgraph-clj StateGraph (marketentry.operation) over an append-only audit ledger; MemStore + langchain-store-backed DatomicStore (marketentry.store, built on io.github.kotoba-lang/langchain-store per ADR-2607141600, not a hand-rolled enc/dec* codec); marketentry.facts grounded in a verified Azerbaijan dossier (etender.gov.az Unified Internet Portal for State Procurement, Law No. 988-VIQ, VÖEN taxpayer ID via the State Tax Service); governor contract + store parity + registry + phase + facts tests all passing (kbb -M:dev:test); 0 clj-kondo errors (kbb -M:lint); CI (.github/workflows/ci.yml) reconstructs the kotoba-lang/langgraph+langchain+langchain-store sibling-checkout layout."
 ;; Deployed into the ai-gftd-repository-dispatch namespace (Workers for
 ;; Platforms). A user Worker there has no URL of its own — it is reachable
 ;; only through the dispatch Worker, so the address is the router plus this
 ;; actor's repository name, which is also the catalog's :repo key.
 ;; /health answers JSON, so it is declared as the probe path.
 :itonami.blueprint/endpoint "https://itonami-fleet-dispatch.04-feasts-minded.workers.dev/cloud-itonami-iso3166-aze"
 :itonami.blueprint/endpoint-kind :dispatch-namespace
 :itonami.blueprint/health-path "/health"}
